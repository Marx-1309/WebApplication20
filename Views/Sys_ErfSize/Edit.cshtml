@model WebApplication20.Models.Sys_ErfSize

@{
    ViewBag.Title = "Create";
}



@*@using (Html.BeginForm())
{*@
    @*@Html.AntiForgeryToken()*@

    <div class="form-horizontal">
        <div class="row">
            <div class="col-xl-6">
                <!-- ====================================
                        ——— 1st FORM
                ===================================== -->

                <div class="card card-default">
                    <div class="card-header">
                        <h2 style="color: rebeccapurple;font-weight: bold">ERF SIZE DETAILS</h2>

                    </div>
                    <div class="card-body">
                        <div class="collapse" id="collapse-from-validation">
                            <pre class="language-html mb-4">
                             </pre>
                        </div>

                        <div>
                            <div class="form-row">
                                @Html.ValidationSummary(true, "", new { @class = "text-danger" })
                                @Html.HiddenFor(model => model.ErfSizeID)
                                <div class="col-xl-6">
                                    <div class="mb-5">
                                        <label class="text-dark font-weight-medium">ERF SIZE</label>
                                        <div class="input-group mb-3">
                                            @Html.EditorFor(model => model.ErfSize, new { htmlAttributes = new { @class = "form-control rounded-pill", @required = "required", @placeholder = "enter erf size" } })
                                            @Html.ValidationMessageFor(model => model.ErfSize, "", new { @class = "text-danger" })
                                        </div>
                                    </div>
                                </div>



                            </div>
                            <br />
                        </div>

                    </div>
                </div>

            </div>

        </div>
        <button id="btnSave" type="submit" class="btn btn-success">Update </button>
        @*<button id="btnSave" class="btn btn-success btn-sm mb-2 float-center" type="button" >*@
    </div>
    <div style="text-align:center;display:none" id="loaderDiv">
        <img src="~/Content/InternetSlowdown_Day.gif" width="150" />
    </div>
@*}*@


<br />

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@*@section Scripts {
        @Scripts.Render("~/bundles/jqueryval")
    }*@


<script src="//cdn.jsdelivr.net/npm/sweetalert2@11"></script>
<script src="sweetalert2.min.js"></script>
<link rel="stylesheet" href="sweetalert2.min.css">
<script src="//cdn.jsdelivr.net/npm/sweetalert2@11"></script>
@*Onclick js and css ref link*@

<script src="~/Scripts/jquery-1.10.2.min.js"></script>
<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/sweetalert/1.1.3/sweetalert.min.css">
<script src="https://cdnjs.cloudflare.com/ajax/libs/sweetalert/1.1.3/sweetalert.min.js"></script>



<script src="https://ajax.googleapis.com/ajax/libs/jquery/3.6.0/jquery.min.js"></script>


<script>
    $(function () {


        $('#btnSave').on('click', function () {

            //  if (!$("#frmApp_Applicant").valid()) return;
            $("#loaderDiv").show();

            var sys_ErfSize = {
                'ErfSizeID': $("#ErfSizeID").val(),
                'ErfSize': $("#ErfSize").val(),

            };



            var model = {
                "sys_ErfSize": sys_ErfSize,

            };

            debugger;
            $.ajax({
                type: 'POST',
                url: '/Sys_ErfSize/Edit/',
                contentType: "application/json; charset=utf-8",
                //contentType: "application/json",
                data: JSON.stringify(model),
                dataType: "JSON",
                cache: false,
                success: function (response) {
                    if (response.status == true) {
                        $("#loaderDiv").hide();
                        swalWithBootstrapButtons.fire(
                            'Created!',
                            'Your record has been Inserted.',
                            'success'
                        )
                        setTimeout(() => {
                            document.location.href = 'https://localhost:44384/Sys_ErfSize/Index';
                        }, 3000);
                    }
                    else {
                        swalWithBootstrapButtons.fire(
                            'Stop!',
                            'Your record was not Updated. Please verify your input',
                            'error'
                        )
                        $("#loaderDiv").hide();
                    }
                }
            });
        });


        const swalWithBootstrapButtons = Swal.mixin({
            customClass: {
                confirmButton: 'btn btn-success',
                cancelButton: 'btn btn-danger'
            },
            buttonsStyling: false
        })

    });
</script>
